#ifndef ADC_DEVICE_H
#define ADC_DEVICE_H

#include "../../device/deviceDescriptor.h"

/**
* Returns a descriptor on the device for the ADC.
* @return a descriptor on the device for the ADC
*/
DeviceDescriptor* getADCDeviceDescriptor();

#endif
